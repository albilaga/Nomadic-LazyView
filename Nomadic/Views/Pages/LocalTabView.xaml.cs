namespace Nomadic.Views.Pages
{
    public partial class LocalTabView
    {
        public LocalTabView()
        {
            InitializeComponent();
            // LazyView.BindingContext = new ViewModels.LocalViewModel();
        }
    }
}